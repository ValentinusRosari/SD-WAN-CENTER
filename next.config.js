/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
      domains: ['randomuser.me', 'isomorphic-furyroad.s3.amazonaws.com'], // Add both hostnames
    },
  };